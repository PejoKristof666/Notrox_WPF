<UserControl x:Class="Notrox.View.Products"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Notrox.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">

        <ItemsControl ItemsSource="{Binding ProductsView}" Margin="30">

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>

                    <Border Width="220"
                        Margin="10"
                        Padding="15"
                        Background="#1E293B"
                        CornerRadius="10">

                        <StackPanel HorizontalAlignment="Center">

                            <TextBlock Text="{Binding Name}"
                                   Foreground="White"
                                   FontWeight="Bold"
                                   FontSize="15"
                                   HorizontalAlignment="Center"
                                   TextWrapping="Wrap"/>

                            <TextBlock Text="{Binding Description}"
                                   Foreground="White"
                                   FontWeight="Bold"
                                   FontSize="10"
                                   HorizontalAlignment="Center"
                                   TextWrapping="Wrap"/>

                            <TextBlock Text="{Binding Price, StringFormat='{}{0} Ft'}"
                                   Foreground="#94A3B8"
                                   FontSize="12"
                                   HorizontalAlignment="Center"
                                   Margin="0 5 0 0"
                                   TextWrapping="Wrap"/>

                            <TextBlock Text="{Binding Ammount, StringFormat=db: \{0\}}"
                                   Foreground="#94A3B8"
                                   FontSize="12"
                                   HorizontalAlignment="Center"
                                   Margin="0 5 0 0"
                                   TextWrapping="Wrap"/>
                            <Button Content="🔧"
                                   Margin="5"
                                   Background="green"
                                   Foreground="White"
                                   BorderThickness="0"
                                   Cursor="Hand"
                                   Command="{Binding DataContext.EditProduct, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                   CommandParameter="{Binding}" />

                            <Button Content="❌"
                                   Margin="5"
                                   Background="White"
                                   Foreground="Red"
                                   BorderThickness="0"
                                   Cursor="Hand"
                                   Command="{Binding DataContext.DeleteProduct, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                   CommandParameter="{Binding}"/>

                        </StackPanel>

                    </Border>

                </DataTemplate>
            </ItemsControl.ItemTemplate>

        </ItemsControl>

    </ScrollViewer>
</UserControl>
