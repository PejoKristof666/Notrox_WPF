<UserControl x:Class="Notrox.View.Users"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto">

        <ItemsControl ItemsSource="{Binding Users}" Margin="30">

            <!-- CARD GRID -->
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <!-- USER CARD -->
            <ItemsControl.ItemTemplate>
                <DataTemplate>

                    <Border Width="220"
                            Margin="10"
                            Padding="15"
                            Background="#1E293B"
                            CornerRadius="10">

                        <StackPanel HorizontalAlignment="Center">

                            <Image Source="{Binding Pfp}"
                                   Width="70"
                                   Height="70"
                                   Margin="0 0 0 10"/>

                            <TextBlock Text="{Binding Username}"
                                       Foreground="White"
                                       FontWeight="Bold"
                                       FontSize="15"
                                       HorizontalAlignment="Center"/>

                            <TextBlock Text="{Binding Email}"
                                       Foreground="#94A3B8"
                                       FontSize="12"
                                       HorizontalAlignment="Center"
                                       Margin="0 5 0 0"/>

                            <TextBlock Text="{Binding Id, StringFormat=#\{0\}}"
                                       Foreground="#94A3B8"
                                       FontSize="12"
                                       HorizontalAlignment="Center"
                                       Margin="0 5 0 0"/>

                            <Button Content="🏠"
                                    Margin="5"
                                    Background="green"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    Command="{Binding DataContext.EditAddresses, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}" />
                            
                            <Button Content="❌"
                                    Margin="5"
                                    Background="White"
                                    Foreground="Red"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    Command="{Binding DataContext.DeleteUser, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"/>
    
                        </StackPanel>
                    </Border>
                    
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>